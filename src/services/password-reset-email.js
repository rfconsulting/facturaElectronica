const env=require('../config/env');
function escapeHtml(value){return String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));}
async function sendPasswordResetEmail({to,fullName,resetUrl}){if(!env.resendApiKey||!env.passwordResetEmailFrom)return{sent:false,reason:'not_configured'};const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${env.resendApiKey}`,'content-type':'application/json','user-agent':'factura-electronica/0.1','idempotency-key':`password-reset-${Date.now()}-${Math.random().toString(16).slice(2)}`},body:JSON.stringify({from:env.passwordResetEmailFrom,to:[to],subject:'Recupera tu contraseña',html:`<p>Hola ${escapeHtml(fullName)},</p><p>Recibimos una solicitud para cambiar tu contraseña.</p><p><a href="${escapeHtml(resetUrl)}">Establecer nueva contraseña</a></p><p>El enlace vence en 30 minutos y solo puede utilizarse una vez. Si no hiciste esta solicitud, ignora este mensaje.</p>`,text:`Hola ${fullName}. Abre este enlace para establecer una nueva contraseña: ${resetUrl}\nEl enlace vence en 30 minutos.`})});if(!response.ok)throw new Error(`El proveedor de correo rechazó la solicitud (${response.status}).`);return{sent:true};}
module.exports={sendPasswordResetEmail};
