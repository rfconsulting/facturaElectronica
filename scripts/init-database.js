const fs = require('node:fs/promises');
const path = require('node:path');
const mysql = require('mysql2/promise');
const env = require('../src/config/env');
const { migrateMultitenancy } = require('./migrate-multitenancy');

(async () => {
  const connection = await mysql.createConnection({ ...env.db, multipleStatements: true });
  try {
    const schema = await fs.readFile(path.join(__dirname, '..', 'database', 'schema.sql'), 'utf8');
    await connection.query(schema);
    const scope = await migrateMultitenancy(connection);
    const [columns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='users' AND COLUMN_NAME IN ('mfa_enabled','mfa_secret_encrypted')", [env.db.database]);
    const existing = new Set(columns.map((column) => column.COLUMN_NAME));
    if (!existing.has('mfa_enabled')) await connection.query('ALTER TABLE users ADD COLUMN mfa_enabled BOOLEAN NOT NULL DEFAULT FALSE AFTER last_login_at');
    if (!existing.has('mfa_secret_encrypted')) await connection.query('ALTER TABLE users ADD COLUMN mfa_secret_encrypted TEXT NULL AFTER mfa_enabled');
    const [superuserColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='users' AND COLUMN_NAME='is_superuser'", [env.db.database]);
    if (!superuserColumns.length) await connection.query('ALTER TABLE users ADD COLUMN is_superuser BOOLEAN NOT NULL DEFAULT FALSE AFTER role, ADD KEY idx_users_superuser (is_superuser,status)');
    const [userStatus] = await connection.query("SELECT COLUMN_TYPE FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='users' AND COLUMN_NAME='status'", [env.db.database]);
    if (userStatus[0] && !userStatus[0].COLUMN_TYPE.includes("'pending'")) await connection.query("ALTER TABLE users MODIFY status ENUM('pending','active','suspended') NOT NULL DEFAULT 'pending'");
    const [invoiceColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='electronic_invoices' AND COLUMN_NAME='customer_id'", [env.db.database]);
    if (!invoiceColumns.length) await connection.query('ALTER TABLE electronic_invoices ADD COLUMN customer_id BIGINT UNSIGNED NULL AFTER created_by, ADD KEY idx_invoice_customer (customer_id), ADD CONSTRAINT fk_invoice_customer FOREIGN KEY (customer_id) REFERENCES clients(id) ON DELETE SET NULL');
    const [idempotencyColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='electronic_invoices' AND COLUMN_NAME IN ('idempotency_key','request_hash')", [env.db.database]);
    const existingIdempotencyColumns = new Set(idempotencyColumns.map((column) => column.COLUMN_NAME));
    if (!existingIdempotencyColumns.has('idempotency_key')) await connection.query('ALTER TABLE electronic_invoices ADD COLUMN idempotency_key VARCHAR(128) COLLATE utf8mb4_bin NULL AFTER created_by');
    if (!existingIdempotencyColumns.has('request_hash')) await connection.query('ALTER TABLE electronic_invoices ADD COLUMN request_hash CHAR(64) COLLATE ascii_bin NULL AFTER idempotency_key');
    const [idempotencyIndexes] = await connection.query("SELECT 1 FROM information_schema.STATISTICS WHERE TABLE_SCHEMA=? AND TABLE_NAME='electronic_invoices' AND INDEX_NAME='uq_invoice_company_idempotency'", [env.db.database]);
    if (!idempotencyIndexes.length) await connection.query('ALTER TABLE electronic_invoices ADD UNIQUE KEY uq_invoice_company_idempotency (company_id,idempotency_key)');
    const [articleColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='articles' AND COLUMN_NAME='available_in_pos'", [env.db.database]);
    if (!articleColumns.length) await connection.query('ALTER TABLE articles ADD COLUMN available_in_pos BOOLEAN NOT NULL DEFAULT FALSE AFTER status, ADD KEY idx_articles_pos (available_in_pos,status)');
    const [leadScoreColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_leads' AND COLUMN_NAME='score'", [env.db.database]);
    if (!leadScoreColumns.length) await connection.query('ALTER TABLE crm_leads ADD COLUMN score TINYINT UNSIGNED NOT NULL DEFAULT 0 AFTER source');
    const [leadStatus] = await connection.query("SELECT COLUMN_TYPE FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_leads' AND COLUMN_NAME='status'", [env.db.database]);
    if (leadStatus[0] && !leadStatus[0].COLUMN_TYPE.includes("'nurturing'")) await connection.query("ALTER TABLE crm_leads MODIFY status ENUM('new','attempting_contact','contacted','qualified','nurturing','discarded','converted') NOT NULL DEFAULT 'new'");
    const [opportunityColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_opportunities'", [env.db.database]);
    const opportunitySet = new Set(opportunityColumns.map((column) => column.COLUMN_NAME));
    if (!opportunitySet.has('contact_id')) await connection.query('ALTER TABLE crm_opportunities ADD COLUMN contact_id BIGINT UNSIGNED NULL AFTER client_id, ADD CONSTRAINT fk_crm_opportunity_contact FOREIGN KEY (contact_id) REFERENCES client_contacts(id) ON DELETE SET NULL');
    if (!opportunitySet.has('service_line')) await connection.query('ALTER TABLE crm_opportunities ADD COLUMN service_line VARCHAR(160) NULL AFTER title');
    const [opportunityStage] = await connection.query("SELECT COLUMN_TYPE FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_opportunities' AND COLUMN_NAME='stage'", [env.db.database]);
    if (opportunityStage[0] && !opportunityStage[0].COLUMN_TYPE.includes("'diagnosis'")) {
      await connection.query("ALTER TABLE crm_opportunities MODIFY stage ENUM('new','contacted','qualified','proposal','negotiation','won','lost','diagnosis','solution_defined','quote_sent','follow_up','payment_pending') NOT NULL DEFAULT 'diagnosis'");
      await connection.query("UPDATE crm_opportunities SET stage=CASE stage WHEN 'new' THEN 'diagnosis' WHEN 'contacted' THEN 'diagnosis' WHEN 'qualified' THEN 'diagnosis' WHEN 'proposal' THEN 'quote_sent' ELSE stage END");
      await connection.query("ALTER TABLE crm_opportunities MODIFY stage ENUM('diagnosis','solution_defined','quote_sent','follow_up','negotiation','payment_pending','won','lost') NOT NULL DEFAULT 'diagnosis'");
    }
    const [activityColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_activities'", [env.db.database]);
    const activitySet = new Set(activityColumns.map((column) => column.COLUMN_NAME));
    if (!activitySet.has('contact_id')) await connection.query('ALTER TABLE crm_activities ADD COLUMN contact_id BIGINT UNSIGNED NULL AFTER client_id, ADD CONSTRAINT fk_crm_activity_contact FOREIGN KEY (contact_id) REFERENCES client_contacts(id) ON DELETE SET NULL');
    if (!activitySet.has('outcome')) await connection.query('ALTER TABLE crm_activities ADD COLUMN outcome VARCHAR(500) NULL AFTER details');
    if (!activitySet.has('next_action')) await connection.query('ALTER TABLE crm_activities ADD COLUMN next_action VARCHAR(250) NULL AFTER outcome');
    const [taskColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_tasks'", [env.db.database]);
    const taskSet = new Set(taskColumns.map((column) => column.COLUMN_NAME));
    if (!taskSet.has('lead_id')) await connection.query('ALTER TABLE crm_tasks ADD COLUMN lead_id BIGINT UNSIGNED NULL AFTER client_id, ADD CONSTRAINT fk_crm_task_lead FOREIGN KEY (lead_id) REFERENCES crm_leads(id) ON DELETE SET NULL');
    if (!taskSet.has('contact_id')) await connection.query('ALTER TABLE crm_tasks ADD COLUMN contact_id BIGINT UNSIGNED NULL AFTER lead_id, ADD CONSTRAINT fk_crm_task_contact FOREIGN KEY (contact_id) REFERENCES client_contacts(id) ON DELETE SET NULL');
    const [invoiceRelationColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='electronic_invoices' AND COLUMN_NAME IN ('source_quote_id','opportunity_id')", [env.db.database]);
    const invoiceRelationSet = new Set(invoiceRelationColumns.map((column) => column.COLUMN_NAME));
    if (!invoiceRelationSet.has('source_quote_id')) await connection.query('ALTER TABLE electronic_invoices ADD COLUMN source_quote_id BIGINT UNSIGNED NULL AFTER customer_id');
    if (!invoiceRelationSet.has('opportunity_id')) await connection.query('ALTER TABLE electronic_invoices ADD COLUMN opportunity_id BIGINT UNSIGNED NULL AFTER source_quote_id');
    const [quoteColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_quotes'", [env.db.database]);
    const quoteSet = new Set(quoteColumns.map((column) => column.COLUMN_NAME));
    if (!quoteSet.has('contact_id')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN contact_id BIGINT UNSIGNED NULL AFTER client_id, ADD CONSTRAINT fk_crm_quote_contact FOREIGN KEY (contact_id) REFERENCES client_contacts(id) ON DELETE SET NULL');
    if (!quoteSet.has('title')) await connection.query("ALTER TABLE crm_quotes ADD COLUMN title VARCHAR(200) NOT NULL DEFAULT 'Cotización comercial' AFTER quote_number");
    if (!quoteSet.has('currency')) await connection.query("ALTER TABLE crm_quotes ADD COLUMN currency CHAR(3) NOT NULL DEFAULT 'USD' AFTER status");
    if (!quoteSet.has('version')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN version INT UNSIGNED NOT NULL DEFAULT 1 AFTER quote_number');
    if (!quoteSet.has('revision_of_id')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN revision_of_id BIGINT UNSIGNED NULL AFTER version, ADD CONSTRAINT fk_crm_quote_revision FOREIGN KEY (revision_of_id) REFERENCES crm_quotes(id) ON DELETE SET NULL');
    if (!quoteSet.has('conversion_policy')) await connection.query("ALTER TABLE crm_quotes ADD COLUMN conversion_policy ENUM('direct_invoice','sales_order') NOT NULL DEFAULT 'sales_order' AFTER currency");
    if (!quoteSet.has('payment_terms')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN payment_terms VARCHAR(500) NULL AFTER valid_until');
    if (!quoteSet.has('customer_snapshot')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN customer_snapshot JSON NULL AFTER payment_terms');
    if (!quoteSet.has('accepted_at')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN accepted_at DATETIME NULL AFTER total');
    if (!quoteSet.has('accepted_by')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN accepted_by BIGINT UNSIGNED NULL AFTER accepted_at, ADD CONSTRAINT fk_crm_quote_acceptor FOREIGN KEY (accepted_by) REFERENCES users(id) ON DELETE SET NULL');
    if (!quoteSet.has('converted_document_type')) await connection.query("ALTER TABLE crm_quotes ADD COLUMN converted_document_type ENUM('invoice_draft','sales_order','invoice') NULL AFTER accepted_by");
    if (!quoteSet.has('converted_document_id')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN converted_document_id BIGINT UNSIGNED NULL AFTER converted_document_type');
    if (!quoteSet.has('converted_at')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN converted_at DATETIME NULL AFTER converted_document_id');
    if (!quoteSet.has('conversion_key')) await connection.query('ALTER TABLE crm_quotes ADD COLUMN conversion_key VARCHAR(128) COLLATE utf8mb4_bin NULL AFTER converted_at, ADD UNIQUE KEY uq_crm_quote_conversion (company_id,conversion_key)');
    const [quoteStatus] = await connection.query("SELECT COLUMN_TYPE FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_quotes' AND COLUMN_NAME='status'", [env.db.database]);
    if (quoteStatus[0] && !quoteStatus[0].COLUMN_TYPE.includes("'pending_approval'")) await connection.query("ALTER TABLE crm_quotes MODIFY status ENUM('draft','pending_approval','approved','sent','viewed','accepted','converted','rejected','expired','cancelled') NOT NULL DEFAULT 'draft'");
    const [quoteNumberIndex] = await connection.query("SELECT COUNT(*) AS columns_count FROM information_schema.STATISTICS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_quotes' AND INDEX_NAME='uq_crm_quote_number'", [env.db.database]);
    if (Number(quoteNumberIndex[0]?.columns_count)===2) await connection.query('ALTER TABLE crm_quotes DROP INDEX uq_crm_quote_number, ADD UNIQUE KEY uq_crm_quote_number (company_id,quote_number,version)');
    const [quoteItemColumns] = await connection.query("SELECT COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_quote_items'", [env.db.database]);
    const quoteItemSet = new Set(quoteItemColumns.map((column) => column.COLUMN_NAME));
    if (!quoteItemSet.has('sku')) await connection.query('ALTER TABLE crm_quote_items ADD COLUMN sku VARCHAR(50) NULL AFTER article_id');
    if (!quoteItemSet.has('unit')) await connection.query("ALTER TABLE crm_quote_items ADD COLUMN unit VARCHAR(20) NOT NULL DEFAULT 'und' AFTER description");
    if (!quoteItemSet.has('discount_percentage')) await connection.query('ALTER TABLE crm_quote_items ADD COLUMN discount_percentage DECIMAL(7,4) NOT NULL DEFAULT 0 AFTER unit_price');
    if (!quoteItemSet.has('discount_amount')) await connection.query('ALTER TABLE crm_quote_items ADD COLUMN discount_amount DECIMAL(13,2) NOT NULL DEFAULT 0 AFTER discount_percentage');
    if (!quoteItemSet.has('tax_rate')) await connection.query('ALTER TABLE crm_quote_items ADD COLUMN tax_rate DECIMAL(7,4) NOT NULL DEFAULT 0 AFTER tax_code');
    if (!quoteItemSet.has('subtotal')) await connection.query('ALTER TABLE crm_quote_items ADD COLUMN subtotal DECIMAL(13,2) NOT NULL DEFAULT 0 AFTER tax_rate');
    if (!quoteItemSet.has('tax_amount')) await connection.query('ALTER TABLE crm_quote_items ADD COLUMN tax_amount DECIMAL(13,2) NOT NULL DEFAULT 0 AFTER subtotal');
    await connection.query(`UPDATE crm_quote_items qi LEFT JOIN articles a ON a.id=qi.article_id SET qi.sku=COALESCE(qi.sku,a.sku),qi.unit=COALESCE(NULLIF(qi.unit,''),a.unit,'und'),qi.subtotal=ROUND(qi.quantity*qi.unit_price-qi.discount_amount,2),qi.tax_rate=CASE qi.tax_code WHEN '01' THEN .07 WHEN '02' THEN .10 WHEN '03' THEN .15 ELSE 0 END,qi.tax_amount=ROUND((qi.quantity*qi.unit_price-qi.discount_amount)*(CASE qi.tax_code WHEN '01' THEN .07 WHEN '02' THEN .10 WHEN '03' THEN .15 ELSE 0 END),2),qi.line_total=ROUND((qi.quantity*qi.unit_price-qi.discount_amount)*(1+CASE qi.tax_code WHEN '01' THEN .07 WHEN '02' THEN .10 WHEN '03' THEN .15 ELSE 0 END),2)`);
    await connection.query(`UPDATE crm_quotes q JOIN clients c ON c.id=q.client_id SET q.customer_snapshot=JSON_OBJECT('id',c.id,'customer_type',c.customer_type,'contributor_type',c.contributor_type,'ruc',c.ruc,'dv',c.dv,'legal_name',c.legal_name,'email',c.email,'phone',c.phone,'address',c.address,'location_code',c.location_code,'province',c.province,'district',c.district,'township',c.township,'country_code',c.country_code,'country_other',c.country_other,'foreign_id_type',c.foreign_id_type,'foreign_id_number',c.foreign_id_number,'foreign_country',c.foreign_country) WHERE q.customer_snapshot IS NULL`);
    const [historyStatus] = await connection.query("SELECT COLUMN_TYPE FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=? AND TABLE_NAME='crm_quote_status_history' AND COLUMN_NAME='to_status'", [env.db.database]);
    if (historyStatus[0] && !historyStatus[0].COLUMN_TYPE.includes("'pending_approval'")) await connection.query("ALTER TABLE crm_quote_status_history MODIFY from_status ENUM('draft','pending_approval','approved','sent','viewed','accepted','converted','rejected','expired','cancelled') NULL, MODIFY to_status ENUM('draft','pending_approval','approved','sent','viewed','accepted','converted','rejected','expired','cancelled') NOT NULL");
    console.log(`Base de datos inicializada. Tenant ${scope.tenantId}, empresa ${scope.companyId}.`);
  } finally { await connection.end(); }
})().catch((error) => { console.error(error.message); process.exitCode = 1; });
