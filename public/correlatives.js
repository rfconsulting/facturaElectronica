async function mountCorrelativeConfiguration(){
  const host=document.querySelector('#configuration-host'); if(!host||document.querySelector('#sequence-form')||!document.querySelector('#fiscal-config-form'))return;
  const panel=document.createElement('section'); panel.className='configuration-panel';
  panel.innerHTML=`<div class="config-heading"><div><span class="eyebrow">Numeración fiscal</span><h2>Correlativos y puntos</h2><p>El correlativo se comparte por sucursal, punto y tipo documental. La asignación determina desde dónde emite cada usuario.</p></div></div><form id="sequence-form"><div class="form-grid"><div class="field"><label>Sucursal</label><input name="branchCode" maxlength="4" required></div><div class="field"><label>Punto</label><input name="billingPoint" pattern="[0-9]{3}" maxlength="3" required></div><div class="field"><label>Tipo documental</label><input name="documentType" pattern="[0-9]{2}" maxlength="2" value="01" required></div><div class="field"><label>Próximo correlativo</label><input name="nextNumber" type="number" min="1" max="9999999999" required></div></div><button class="submit-button" type="submit">Guardar correlativo</button></form><div id="sequence-list"></div><form id="assignment-form"><div class="form-grid"><div class="field wide"><label>Usuario</label><select name="userId" required></select></div><div class="field"><label>Sucursal</label><input name="branchCode" maxlength="4" required></div><div class="field"><label>Punto</label><input name="billingPoint" pattern="[0-9]{3}" maxlength="3" required></div></div><button class="submit-button" type="submit">Asignar punto</button></form><div id="assignment-list"></div><p class="form-message" role="alert"></p>`;
  host.append(panel); const message=panel.querySelector('.form-message'),securityForm=document.querySelector('#fiscal-config-form');
  async function refresh(){const data=await request('/api/config/correlatives');panel.querySelector('[name="userId"]').innerHTML=data.users.map(u=>`<option value="${u.id}">${escapeHtml(u.fullName)} · ${escapeHtml(u.email)}</option>`).join('');panel.querySelector('#sequence-list').innerHTML=data.sequences.map(s=>`<p>${escapeHtml(s.branchCode)} / ${escapeHtml(s.billingPoint)} / tipo ${escapeHtml(s.documentType)} · próximo <strong>${s.nextNumber}</strong></p>`).join('')||'<p>Se usa el correlativo automático.</p>';panel.querySelector('#assignment-list').innerHTML=data.assignments.map(a=>`<p>${escapeHtml(a.fullName)} · ${escapeHtml(a.branchCode)} / ${escapeHtml(a.billingPoint)}</p>`).join('')||'<p>Los usuarios usan el punto general.</p>';}
  for(const [selector,url] of [['#sequence-form','/api/config/correlatives/sequence'],['#assignment-form','/api/config/correlatives/assignment']])panel.querySelector(selector).addEventListener('submit',async event=>{event.preventDefault();if(!event.currentTarget.reportValidity())return;try{await secureConfigAction(()=>request(url,{method:'PUT',headers:{'content-type':'application/json','x-csrf-token':csrfToken},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))}),securityForm);message.className='form-message success';message.textContent='Configuración actualizada.';await refresh();}catch(error){message.className='form-message error';message.textContent=error.message;}});
  await refresh();
}
new MutationObserver(()=>mountCorrelativeConfiguration().catch(()=>{})).observe(document.querySelector('#configuration-host'),{childList:true});
