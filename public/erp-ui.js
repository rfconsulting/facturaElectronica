let erpData={dashboard:null,quotations:[],orders:[],receivables:[]};
const erpStatus={draft:'Borrador',pending_approval:'Por aprobar',approved:'Aprobada',sent:'Enviada',viewed:'Vista',accepted:'Aceptada',converted:'Convertida',rejected:'Rechazada',expired:'Vencida',cancelled:'Cancelada',confirmed:'Confirmado',partially_invoiced:'Facturación parcial',invoiced:'Facturado',pending:'Pendiente',partial:'Pago parcial',overdue:'Vencida',paid:'Pagada'};
const money=value=>`B/. ${Number(value||0).toFixed(2)}`;
function erpEmpty(message){return `<div class="erp-empty"><strong>${escapeHtml(message)}</strong><span>Los registros aparecerán aquí cuando estén disponibles.</span></div>`;}
function setupErpWorkspace(){const tabs=document.querySelector('.erp-area-tabs');if(!tabs||document.querySelector('[data-panel="erpDashboard"]'))return;tabs.innerHTML=`<button type="button" data-section="erpDashboard">Resumen</button><button type="button" data-section="quotations">Cotizaciones</button><button type="button" data-section="orders">Pedidos</button><button type="button" data-section="documents">Facturas</button><button type="button" data-section="pos">POS</button><button type="button" data-section="clients">Clientes</button><button type="button" data-section="articles">Artículos</button><button type="button" data-section="receivables">Cobros</button>`;document.querySelector('.nav-module-link[data-module="erp"]').dataset.section='erpDashboard';const host=document.querySelector('.app-content');for(const [name,html] of [['erpDashboard','<div id="erp-dashboard"></div>'],['quotations','<div id="erp-quotations"></div>'],['orders','<div id="erp-orders"></div>'],['receivables','<div id="erp-receivables"></div>']]){const section=document.createElement('section');section.className='app-section erp-module';section.dataset.panel=name;section.hidden=true;section.innerHTML=html;host.append(section);}document.querySelectorAll('[data-area-navigation="erp"] [data-section],.nav-module-link[data-module="erp"]').forEach(button=>button.addEventListener('click',()=>navigate(button.dataset.section)));host.addEventListener('click',handleErpClick);host.addEventListener('submit',handleErpSubmit);}
async function loadErpData(){const [dashboard,quotations,orders,receivables]=await Promise.all([request('/api/erp/dashboard'),request('/api/quotations'),request('/api/erp/orders'),request('/api/crm/receivables')]);erpData={dashboard,quotations:quotations.quotes,orders:orders.orders,receivables:receivables.receivables};}
function renderErpDashboard(){const d=erpData.dashboard;document.querySelector('#erp-dashboard').innerHTML=`<div class="page-heading"><div><p class="eyebrow">Operación integral</p><h1>Resumen ERP</h1><p>Prioridades de ventas, facturación y cobranza de la empresa activa.</p></div><button class="submit-button" data-erp-new-quote>+ Nueva cotización</button></div><div class="erp-kpis"><button data-section="quotations"><span>Cotizaciones abiertas</span><strong>${Number(d.quotes.openCount||0)}</strong><small>${Number(d.quotes.pendingApproval||0)} por aprobar</small></button><button data-section="orders"><span>Pedidos por facturar</span><strong>${Number(d.orders.confirmed||0)}</strong><small>${Number(d.orders.total||0)} pedidos totales</small></button><button data-section="receivables"><span>Saldo por cobrar</span><strong>${money(d.receivables.balance)}</strong><small>${Number(d.receivables.total||0)} cuentas abiertas</small></button><button data-section="documents"><span>Ventas del mes</span><strong>${money(d.sales.total)}</strong><small>${Number(d.sales.documents||0)} facturas autorizadas</small></button></div><div class="erp-flow"><header><div><span class="eyebrow">Flujo operativo</span><h2>De la oferta al cobro</h2></div></header><div class="erp-flow-steps">${[['quotations','Cotizaciones',d.quotes.openCount],['orders','Pedidos',d.orders.confirmed],['documents','Facturas',d.sales.documents],['receivables','Cobros',d.receivables.total]].map(([section,label,count],i)=>`<button data-section="${section}"><i>${i+1}</i><span>${label}</span><strong>${Number(count||0)}</strong></button>`).join('')}</div></div>`;}
function renderQuotations(){document.querySelector('#erp-quotations').innerHTML=`<div class="page-heading"><div><p class="eyebrow">Ventas</p><h1>Cotizaciones</h1><p>Prepara, aprueba y convierte ofertas comerciales sin duplicar información.</p></div><button class="submit-button" data-erp-new-quote>+ Nueva cotización</button></div><div class="erp-list-card"><div class="erp-list-head"><span>Cotización</span><span>Cliente</span><span>Estado</span><span>Total</span><span></span></div>${erpData.quotations.map(q=>`<div class="erp-list-row"><div><strong>${escapeHtml(q.quoteNumber)} · v${q.version||1}</strong><small>${escapeHtml(q.title)}</small></div><span>${escapeHtml(q.clientName)}</span><span><i class="erp-status status-${q.status}">${erpStatus[q.status]||q.status}</i></span><strong>${money(q.total)}</strong><div class="erp-row-actions"><button class="small-button" data-erp-quote="${q.id}">Ver</button>${q.status==='draft'?`<button class="small-button" data-erp-edit-quote="${q.id}">Editar</button>`:''}</div></div>`).join('')||erpEmpty('No hay cotizaciones')}</div>`;}
function renderOrders(){document.querySelector('#erp-orders').innerHTML=`<div class="page-heading"><div><p class="eyebrow">Ejecución comercial</p><h1>Pedidos</h1><p>Pedidos confirmados que nacen de cotizaciones aceptadas.</p></div></div><div class="erp-list-card"><div class="erp-list-head"><span>Pedido</span><span>Cliente</span><span>Estado</span><span>Total</span><span></span></div>${erpData.orders.map(o=>`<div class="erp-list-row"><div><strong>${escapeHtml(o.orderNumber)}</strong><small>${escapeHtml(o.quoteNumber)} · v${o.quoteVersion}</small></div><span>${escapeHtml(o.clientName)}</span><span><i class="erp-status status-${o.status}">${erpStatus[o.status]||o.status}</i></span><strong>${money(o.total)}</strong><div class="erp-row-actions"><button class="small-button" data-erp-order="${o.id}">Ver</button>${['confirmed','partially_invoiced'].includes(o.status)?`<button class="submit-button" data-erp-invoice-order="${o.id}">Facturar</button>`:''}</div></div>`).join('')||erpEmpty('No hay pedidos')}</div>`;}
function renderReceivables(){document.querySelector('#erp-receivables').innerHTML=`<div class="page-heading"><div><p class="eyebrow">Finanzas</p><h1>Cuentas por cobrar</h1><p>Controla saldos y registra pagos de facturas autorizadas.</p></div></div><div class="erp-list-card"><div class="erp-list-head"><span>Factura</span><span>Cliente</span><span>Estado</span><span>Saldo</span><span></span></div>${erpData.receivables.map(r=>`<div class="erp-list-row"><div><strong>${escapeHtml(r.fiscalNumber)}</strong><small>Total ${money(r.originalAmount)}</small></div><span>${escapeHtml(r.clientName||'Consumidor final')}</span><span><i class="erp-status status-${r.status}">${erpStatus[r.status]||r.status}</i></span><strong>${money(r.balance)}</strong><div>${r.status!=='paid'?`<button class="submit-button" data-erp-pay="${r.id}">Registrar pago</button>`:''}</div></div>`).join('')||erpEmpty('No hay cuentas por cobrar')}</div>`;}
async function loadErpSection(section){if(!['erpDashboard','quotations','orders','receivables'].includes(section))return;try{await loadErpData();renderErpDashboard();renderQuotations();renderOrders();renderReceivables();}catch(error){const target=document.querySelector(`[data-panel="${section}"]>div`);if(target)target.innerHTML=erpEmpty(error.message);}}
function erpDialog(title,body){let dialog=document.querySelector('#erp-dialog');if(!dialog){dialog=document.createElement('dialog');dialog.id='erp-dialog';dialog.className='erp-dialog';document.body.append(dialog);}dialog.classList.remove('erp-quote-dialog');dialog.innerHTML=`<header><div><span class="eyebrow">ERP</span><h2>${escapeHtml(title)}</h2></div><button type="button" data-erp-close>×</button></header><div>${body}</div>`;dialog.showModal();}
async function handleErpClick(event){const b=event.target.closest('button');if(!b)return;if(b.dataset.section){navigate(b.dataset.section);return;}try{if(b.hasAttribute('data-erp-new-quote')){await openQuoteEditor();return;}if(b.dataset.erpEditQuote){await openQuoteEditor(b.dataset.erpEditQuote);return;}if(b.dataset.erpQuote){await openQuoteDetail(b.dataset.erpQuote);return;}if(b.dataset.erpOrder){const {order}=await request(`/api/erp/orders/${b.dataset.erpOrder}`);erpDialog(`Pedido ${order.orderNumber}`,`<p><strong>${escapeHtml(order.clientName)}</strong> · Cotización ${escapeHtml(order.quoteNumber)} v${order.quoteVersion}</p><div class="erp-order-lines">${order.items.map(i=>`<div><span>${escapeHtml(i.description)}<small>${Number(i.quantity)} ${escapeHtml(i.unit)} × ${money(i.unitPrice)}</small></span><strong>${money(i.lineTotal)}</strong></div>`).join('')}</div><div class="erp-total"><span>Total</span><strong>${money(order.total)}</strong></div>`);return;}if(b.dataset.erpInvoiceOrder){const {draft}=await request(`/api/erp/orders/${b.dataset.erpInvoiceOrder}/invoice-draft`);invoiceCommercialSource={sourceQuoteId:draft.sourceQuoteId,opportunityId:draft.opportunityId};navigate('invoice');document.querySelector('#invoice-client').value=String(draft.clientId);await selectInvoiceClient(draft.clientId);document.querySelector('#items').innerHTML='';for(const item of draft.items){addItem(item.articleId||'');const row=document.querySelector('#items').lastElementChild;row.querySelector('[data-field="description"]').value=item.description;row.querySelector('[data-field="quantity"]').value=item.quantity;row.querySelector('[data-field="unitPrice"]').value=item.unitPrice;row.querySelector('[data-field="taxCode"]').value=item.taxCode;}calculate();document.querySelector('#invoice-message').textContent='Pedido cargado. Revisa y emite la factura electrónica.';return;}if(b.dataset.erpPay){const row=erpData.receivables.find(r=>String(r.id)===b.dataset.erpPay);erpDialog('Registrar pago',`<form data-erp-payment="${row.id}"><p>Saldo disponible: <strong>${money(row.balance)}</strong></p><label>Importe<input name="amount" type="number" min="0.01" max="${row.balance}" step="0.01" required></label><label>Forma de pago<select name="paymentMethod"><option value="transfer">Transferencia</option><option value="cash">Efectivo</option><option value="credit_card">Tarjeta de crédito</option><option value="debit_card">Tarjeta de débito</option><option value="check">Cheque</option><option value="other">Otra</option></select></label><label>Referencia<input name="reference" maxlength="120"></label><button class="submit-button">Guardar pago</button></form>`);return;}if(b.hasAttribute('data-erp-close'))b.closest('dialog').close();}catch(error){alert(error.message);}}
async function handleErpSubmit(event){const form=event.target.closest('[data-erp-payment]');if(!form)return;event.preventDefault();try{await request(`/api/crm/receivables/${form.dataset.erpPayment}/payments`,{method:'POST',headers:{'content-type':'application/json','x-csrf-token':csrfToken},body:JSON.stringify(Object.fromEntries(new FormData(form)))});form.closest('dialog').close();await loadErpSection('receivables');}catch(error){alert(error.message);}}
let erpQuoteClients=[],erpQuoteContacts=[],erpQuoteOpportunities=[];
async function loadErpQuoteReferences(){const [clients,contacts,opportunities]=await Promise.all([request('/api/clients'),request('/api/crm/contacts'),request('/api/crm/opportunities')]);erpQuoteClients=clients.clients;erpQuoteContacts=contacts.contacts;erpQuoteOpportunities=opportunities.opportunities;}
const erpOptions=(items,value,label,blank)=>(blank?`<option value="">${blank}</option>`:'')+items.map(item=>`<option value="${item[value]}">${escapeHtml(item[label])}</option>`).join('');
async function openErpQuoteEditor(quoteId=null){await loadErpQuoteReferences();let quote=null;if(quoteId)({quote}=await request(`/api/quotations/${quoteId}`));const tomorrow=new Date(Date.now()+15*86400000).toISOString().slice(0,10);erpDialog(quote?'Editar cotización':'Nueva cotización',`<form data-erp-quote-form data-quote-id="${quoteId||''}"><div class="erp-quote-fields"><label>Título *<input name="title" maxlength="200" value="${escapeHtml(quote?.title||'Propuesta comercial')}" required></label><label>Cliente *<select name="clientId" required>${erpOptions(erpQuoteClients,'id','legalName','Seleccionar cliente')}</select></label><label>Contacto<select name="contactId">${erpOptions(erpQuoteContacts,'id','fullName','Sin contacto')}</select></label><label>Oportunidad<select name="opportunityId">${erpOptions(erpQuoteOpportunities,'id','title','Sin oportunidad')}</select></label><label>Válida hasta<input name="validUntil" type="date" value="${quote?.validUntil?String(quote.validUntil).slice(0,10):tomorrow}"></label><label>Moneda<input name="currency" maxlength="3" value="${escapeHtml(quote?.currency||'USD')}" required></label><label>Condiciones de pago<input name="paymentTerms" maxlength="500" value="${escapeHtml(quote?.paymentTerms||'')}"></label><label>Al aceptar<select name="conversionPolicy"><option value="sales_order">Crear pedido</option><option value="direct_invoice">Preparar factura directa</option></select></label><label class="wide">Notas<textarea name="notes" maxlength="2000" rows="3">${escapeHtml(quote?.notes||'')}</textarea></label></div><div class="quote-items-heading"><strong>Renglones</strong><button class="small-button" type="button" data-erp-quote-add>+ Agregar renglón</button></div><div data-quote-items>${(quote?.items?.length?quote.items:[{}]).map(quoteItemRow).join('')}</div><div class="pos-summary"><div><span>Subtotal</span><strong data-quote-subtotal></strong></div><div><span>ITBMS</span><strong data-quote-tax></strong></div><div class="pos-total"><span>Total</span><strong data-quote-total></strong></div></div><p class="form-message" data-erp-quote-message role="alert"></p><button class="submit-button">${quote?'Guardar borrador':'Crear cotización'}</button></form>`);const form=document.querySelector('[data-erp-quote-form]');form.elements.clientId.value=String(quote?.clientId||'');form.elements.contactId.value=String(quote?.contactId||'');form.elements.opportunityId.value=String(quote?.opportunityId||'');form.elements.conversionPolicy.value=quote?.conversionPolicy||'sales_order';form.closest('dialog').classList.add('erp-quote-dialog');calculateQuote(form);}
const erpQuoteActions=quote=>({draft:[['pending_approval','Solicitar aprobación']],pending_approval:[['approved','Aprobar'],['draft','Devolver a borrador']],approved:[['sent','Emitir cotización']],sent:[['viewed','Marcar como vista'],['accepted','Aceptar'],['rejected','Rechazar']],viewed:[['accepted','Aceptar'],['rejected','Rechazar']]}[quote.status]||[]).map(([status,label])=>`<button class="small-button" type="button" data-erp-quote-status="${status}" data-quote-id="${quote.id}">${label}</button>`).join('');
async function openErpQuoteDetail(quoteId){const {quote}=await request(`/api/quotations/${quoteId}`);erpDialog(`Cotización ${quote.quoteNumber} · v${quote.version||1}`,`<div class="erp-quote-detail"><div class="erp-quote-meta"><div><span>Cliente</span><strong>${escapeHtml(quote.clientName)}</strong></div><div><span>Estado</span><i class="erp-status status-${quote.status}">${erpStatus[quote.status]||escapeHtml(quote.status)}</i></div><div><span>Válida hasta</span><strong>${quote.validUntil?new Date(quote.validUntil).toLocaleDateString('es-PA'):'Sin fecha'}</strong></div></div><h3>${escapeHtml(quote.title)}</h3><div class="erp-order-lines">${quote.items.map(item=>`<div><span>${escapeHtml(item.description)}<small>${Number(item.quantity)} × ${money(item.unitPrice)} · Desc. ${Number(item.discountPercentage||0).toFixed(2)}%</small></span><strong>${money(item.lineTotal)}</strong></div>`).join('')}</div><div class="pos-summary"><div><span>Subtotal</span><strong>${money(quote.subtotal)}</strong></div><div><span>ITBMS</span><strong>${money(quote.taxTotal)}</strong></div><div class="pos-total"><span>Total</span><strong>${money(quote.total)}</strong></div></div>${quote.paymentTerms?`<p><strong>Condiciones:</strong> ${escapeHtml(quote.paymentTerms)}</p>`:''}${quote.notes?`<p>${escapeHtml(quote.notes)}</p>`:''}<div class="erp-quote-actions">${quote.status==='draft'?`<button class="small-button" type="button" data-erp-edit-quote="${quote.id}">Editar</button>`:''}${erpQuoteActions(quote)}${['sent','viewed','rejected','expired'].includes(quote.status)?`<button class="small-button" type="button" data-erp-quote-revise="${quote.id}">Nueva revisión</button>`:''}${quote.status==='accepted'?`<button class="submit-button" type="button" data-erp-quote-convert="${quote.id}">Convertir</button>`:''}<button class="small-button" type="button" data-erp-quote-duplicate="${quote.id}">Duplicar</button></div><h3>Historial</h3><div class="erp-quote-history">${quote.history.map(item=>`<p><span>${erpStatus[item.toStatus]||escapeHtml(item.toStatus)}</span><small>${escapeHtml(item.changedBy)} · ${new Date(item.changedAt).toLocaleString('es-PA')}${item.reason?` · ${escapeHtml(item.reason)}`:''}</small></p>`).join('')}</div></div>`);}
async function erpQuoteRequest(url,options={}){return request(url,{...options,headers:{'content-type':'application/json','x-csrf-token':csrfToken,...options.headers}});}
async function loadErpQuoteDraft(draft){invoiceCommercialSource={sourceQuoteId:draft.sourceQuoteId,opportunityId:draft.opportunityId};navigate('invoice');document.querySelector('#invoice-client').value=String(draft.clientId);await selectInvoiceClient(draft.clientId);document.querySelector('#items').innerHTML='';for(const item of draft.items){addItem(item.articleId||'');const row=document.querySelector('#items').lastElementChild;row.querySelector('[data-field="description"]').value=item.description;row.querySelector('[data-field="quantity"]').value=item.quantity;row.querySelector('[data-field="unitPrice"]').value=item.unitPrice;row.querySelector('[data-field="taxCode"]').value=item.taxCode;}calculate();document.querySelector('#invoice-message').textContent='Cotización cargada. Revisa y emite la factura electrónica.';}
const baseErpClick=handleErpClick;
handleErpClick=async function(event){const button=event.target.closest('button');if(!button)return;try{if(button.hasAttribute('data-erp-new-quote')){await openErpQuoteEditor();return;}if(button.dataset.erpEditQuote){await openErpQuoteEditor(button.dataset.erpEditQuote);return;}if(button.dataset.erpQuote){await openErpQuoteDetail(button.dataset.erpQuote);return;}if(button.hasAttribute('data-erp-quote-add')){const form=button.closest('form');form.querySelector('[data-quote-items]').insertAdjacentHTML('beforeend',quoteItemRow());calculateQuote(form);return;}if(button.hasAttribute('data-quote-remove')&&button.closest('[data-erp-quote-form]')){const form=button.closest('form');if(form.querySelectorAll('.quote-item-row').length>1)button.closest('.quote-item-row').remove();calculateQuote(form);return;}if(button.dataset.erpQuoteStatus){await erpQuoteRequest(`/api/quotations/${button.dataset.quoteId}/status`,{method:'POST',body:JSON.stringify({status:button.dataset.erpQuoteStatus})});button.closest('dialog')?.close();await loadErpSection('quotations');return;}if(button.dataset.erpQuoteDuplicate){await erpQuoteRequest(`/api/quotations/${button.dataset.erpQuoteDuplicate}/duplicate`,{method:'POST',body:'{}'});button.closest('dialog')?.close();await loadErpSection('quotations');return;}if(button.dataset.erpQuoteRevise){await erpQuoteRequest(`/api/quotations/${button.dataset.erpQuoteRevise}/revisions`,{method:'POST',body:'{}'});button.closest('dialog')?.close();await loadErpSection('quotations');return;}if(button.dataset.erpQuoteConvert){const result=await erpQuoteRequest(`/api/quotations/${button.dataset.erpQuoteConvert}/convert`,{method:'POST',headers:{'Idempotency-Key':`erp-quotation-${button.dataset.erpQuoteConvert}-${Date.now()}`},body:'{}'});button.closest('dialog')?.close();if(result.draft)await loadErpQuoteDraft(result.draft);else{await loadErpSection('orders');navigate('orders');}return;}return baseErpClick(event);}catch(error){const message=button.closest('dialog')?.querySelector('[data-erp-quote-message]');if(message){message.className='form-message error';message.textContent=[error.message,...(error.details||[])].join(' ');}else alert(error.message);}};
const baseErpSubmit=handleErpSubmit;
handleErpSubmit=async function(event){const form=event.target.closest('[data-erp-quote-form]');if(!form)return baseErpSubmit(event);event.preventDefault();event.stopImmediatePropagation();const button=form.querySelector('[type="submit"]'),message=form.querySelector('[data-erp-quote-message]');button.disabled=true;try{const data=Object.fromEntries(new FormData(form));data.items=quoteItemsFrom(form);const quoteId=form.dataset.quoteId;await erpQuoteRequest(quoteId?`/api/quotations/${quoteId}`:'/api/quotations',{method:quoteId?'PUT':'POST',body:JSON.stringify(data)});form.closest('dialog').close();await loadErpSection('quotations');navigate('quotations');}catch(error){message.className='form-message error';message.textContent=[error.message,...(error.details||[])].join(' ');}finally{button.disabled=false;}};
document.addEventListener('input',event=>{const form=event.target.closest('[data-erp-quote-form]');if(!form)return;if(event.target.hasAttribute('data-quote-article')){const article=articleCatalog.find(item=>String(item.id)===event.target.value),row=event.target.closest('.quote-item-row');if(article){row.querySelector('[data-quote-field="description"]').value=article.description||article.name;row.querySelector('[data-quote-field="unitPrice"]').value=Number(article.salePrice).toFixed(2);row.querySelector('[data-quote-field="taxCode"]').value=article.taxCode;}}calculateQuote(form);});
setupErpWorkspace();
